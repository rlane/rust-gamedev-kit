before_install:
  - yes | sudo add-apt-repository ppa:hansjorg/rust
  - sudo apt-get update
install:
  - sudo apt-get install rust-nightly cmake xorg-dev libglu1-mesa-dev
script:
  -  git submodule update --init && ./build-libraries.sh
